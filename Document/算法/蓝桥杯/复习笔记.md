## 蓝桥杯复习资料

[TOC]

------

### Day1：模拟基础

#### 一、输入输出

- 万能头：#include<bits/stdc++.h>

- cin与cout作为输入输出函数的缺点是**速度较慢**，所以需要用指令进行加速（关同步），关同步后就不要同时使用cin/cout和scanf/printf

```c++
#include<bits/stdc++.h>

using namespace std;

int main()
{
    //关同步指令如下：
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    //关同步指令如上
    int x,y;
    cin >> x >> y;
    cout << x << endl << y;
    return 0;
}
```

#### 二、多组输入

​	我们在输入输出时要遵守题目的输入输出格式规范，常见的有以下几种输入方式

##### 1）有条件的多组输入

![image-20260311222851486](C:\Users\Duuuzx\AppData\Roaming\Typora\typora-user-images\image-20260311222851486.png)

```c++
#include<bits/stdc++/h>
using namespace std;
int main()
{
	int n,num_even = 0,sum_odd=0;
    while(cin >> n)
    {
        if(n==0) break;
        else
        {
            if(n%2 == 0) num_even++;
            else sum_odd += n;
        }
    }
    cout << num_even << endl << sum_odd;
}
```

##### 2）无条件的多组输入

​	题目同上，但是删去了以0作为循环结束的条件

- 利用while循环进行输入，但是要退出就要输入ctrl+z再按下回车

```c++
#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,num_even = 0,sum_odd=0;
    while(cin >> n)//当没有告诉我们要输入多少个数字时，这句语句可以来处理非常繁琐的输入
    {
        if(n%2 ==0) num_even++;
        else sum_odd +=n;
    }
    cout << num_even << endl << sum_odd;
    return 0;
}
```

#### 三、getline读入带空格的字符串

------

#### 星系炸弹问题

```c++
#include<bits/stdc++.h>
using namespace std;
int main()
{
    int flag = 0 , cnt = 0;
    #以上解决问题专属
    for(year=2014;year<=2022;year++)
    for(month=1;month<=12;month++)
    for(day=1;day<=31;day++)
    {
        if(month== 1 || month == 3 || month == 5 || month == 7 || month == 8 || month==10 ||month==12);
        else if(month ==2)
        {
            if((year%4==0 && year%100 != 0)||year%400 == 0)
            {
                if(day>29) break;
            }
            else
            {
                if(day>28) break;
            }
        }
        else
        {
            if(day>30) break;
        }
        //新问题
        if(year==2014 && month == 1 && day == 9)
        {
            flag = 1;
        }
        if(flag == 1) cnt++;
        if(cnt == 1001)
        {
            printf("%d-%02d-%02d",year,month,day);
            break;
        }
    }
    return 0;
}
```

